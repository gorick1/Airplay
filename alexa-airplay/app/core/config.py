# config.py is no longer needed — all config is handled via
# environment variables (SUPERVISOR_TOKEN, HA_URL) and
# the HA Supervisor API.  Kept as an empty module so any
# stale imports don't crash on startup.
